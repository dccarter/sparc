dist: xenial
branches:
  only:
    - master

language: cpp

compiler:
  - gcc
  
before_install:
  - sudo apt-get purge cmake
  - sudo apt-get update -q
  
install:
  - sudo apt-get install cmake -y
  - sudo apt-get install cmake=3.5.2-2ubuntu1
  - cmake --version
  
before_script:
  - mkdir build
  - cd build

script:
 - cmake ..
 - make && ./sparc_test
 
cache:
  - apt
